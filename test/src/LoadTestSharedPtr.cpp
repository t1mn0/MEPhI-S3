#include <gtest/gtest.h>

#include "../../include/SmartPtr/SharedPtr.hpp"
#include <string>

// 

TEST(LoadTestSharedPtr, Creating1000){
    for (int i = 0; i < 1000; ++i){
        // std::string* raw_ptr = new std::string("string");
        for (int j = 0; j < 1000; ++j){

        }
    }
}